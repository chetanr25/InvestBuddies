import 'package:flutter/material.dart';

class YourInvestmentScreen extends StatelessWidget {
  const YourInvestmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Your Investment Screen'),
    );
  }
}
